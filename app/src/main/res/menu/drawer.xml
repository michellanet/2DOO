<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- group with single selected item so only one item is highlighted in the nav menu -->
    <group
        android:id="@+id/nav_group_main"
        android:checkableBehavior="single">
        <item
            android:id="@+id/drawer_item_profile"
            android:checked="true"
            android:icon="@drawable/ic_face_24px"
            android:title="Profile"/>

        <item
            android:id="@+id/drawer_item_categories"
            android:icon="@drawable/ic_list_24px"
            android:title="Select Categories"/>

        <item
            android:id="@+id/drawer_item_favs"
            android:icon="@drawable/ic_favorite_border_24px"
            android:title="Favorite Restaurants"/>

        <item
            android:id="@+id/drawer_item_history"
            android:icon="@drawable/ic_history_24px"
            android:title="Reservation History"/>

    </group>

    <group
        android:id="@+id/nav_group_other"
        android:checkableBehavior="single">

        <item
            android:id="@+id/drawer_item_settings"
            android:icon="@drawable/ic_settings_24px"
            android:title="Settings"/>

        <item
            android:id="@+id/drawer_item_about"
            android:icon="@drawable/ic_help_outline_24px"
            android:title="About"/>
    </group>
</menu>